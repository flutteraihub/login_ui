# AGENTS.md

## Cursor Cloud specific instructions

This repository (`login_ui`) is currently an empty project with only a `README.md`.
There are no source files, dependencies, build scripts, or services to run.

When code is added to this repository, update this section with:
- How to install dependencies
- How to run the dev server
- How to run tests and lint
- Any non-obvious caveats discovered during setup
