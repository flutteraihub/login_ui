# Login UI

A Flutter sample app with a polished, responsive login screen.

## Features

- Material 3 theme with a soft gradient background.
- Email and password validation.
- Password visibility toggle.
- Remember-me checkbox and secondary account actions.

## Run locally

Install Flutter, then run:

```bash
flutter pub get
flutter run
```

Run widget tests with:

```bash
flutter test
```
